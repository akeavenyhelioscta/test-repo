"""Naive baselines targeting the same hub as ``like_day_model_knn.pjm_rto_hourly``.

Hub follows ``like_day_model_knn.configs.HUB`` so leaderboard numbers
remain directly comparable to the like-day model.
"""
