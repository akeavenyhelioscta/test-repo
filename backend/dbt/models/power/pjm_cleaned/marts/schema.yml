version: 2

models:
  - name: pjm_lmps_hourly
    description: '{{ doc("pjm_mart_lmps_hourly") }}'
    columns:
      - name: datetime_beginning_utc
        description: "Start of the hour interval (UTC)."
      - name: datetime_ending_utc
        description: "End of the hour interval (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: datetime_beginning_local
        description: "Start of the hour interval (local time, see timezone column)."
      - name: datetime_ending_local
        description: "End of the hour interval (local time, see timezone column)."
      - name: date
        description: '{{ doc("col_date") }}'
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'
      - name: hub
        description: "PJM pricing hub name."
      - name: market
        description: '{{ doc("col_market") }}'

  - name: pjm_lmps_daily
    description: '{{ doc("pjm_mart_lmps_daily") }}'
    columns:
      - name: date
        description: '{{ doc("col_date") }}'
      - name: hub
        description: "PJM pricing hub name."
      - name: period
        description: '{{ doc("col_period") }}'
      - name: market
        description: '{{ doc("col_market") }}'

  - name: pjm_load_da_hourly
    description: '{{ doc("pjm_mart_load_da_hourly") }}'
    columns:
      - name: datetime_beginning_utc
        description: "Start of the hour interval (UTC)."
      - name: datetime_ending_utc
        description: "End of the hour interval (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: datetime_beginning_local
        description: "Start of the hour interval (local time, see timezone column)."
      - name: datetime_ending_local
        description: "End of the hour interval (local time, see timezone column)."
      - name: date
        description: '{{ doc("col_date") }}'
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'
      - name: region
        description: '{{ doc("col_region") }}'
      - name: da_load_mw
        description: "Day-ahead cleared load (MW)."

  - name: pjm_load_rt_metered_hourly
    description: '{{ doc("pjm_mart_load_rt_metered_hourly") }}'
    columns:
      - name: datetime_beginning_utc
        description: "Start of the hour interval (UTC)."
      - name: datetime_ending_utc
        description: "End of the hour interval (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: datetime_beginning_local
        description: "Start of the hour interval (local time, see timezone column)."
      - name: datetime_ending_local
        description: "End of the hour interval (local time, see timezone column)."
      - name: date
        description: '{{ doc("col_date") }}'
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'
      - name: region
        description: '{{ doc("col_region") }}'
      - name: rt_load_mw
        description: "Metered real-time load (MW)."

  - name: pjm_load_rt_prelim_hourly
    description: '{{ doc("pjm_mart_load_rt_prelim_hourly") }}'
    columns:
      - name: datetime_beginning_utc
        description: "Start of the hour interval (UTC)."
      - name: datetime_ending_utc
        description: "End of the hour interval (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: datetime_beginning_local
        description: "Start of the hour interval (local time, see timezone column)."
      - name: datetime_ending_local
        description: "End of the hour interval (local time, see timezone column)."
      - name: date
        description: '{{ doc("col_date") }}'
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'
      - name: region
        description: '{{ doc("col_region") }}'
      - name: rt_load_mw
        description: "Preliminary real-time metered load (MW)."

  - name: pjm_load_forecast_hourly
    description: '{{ doc("pjm_mart_load_forecast_hourly") }}'
    columns:
      - name: forecast_rank
        description: '{{ doc("col_forecast_rank") }}'
      - name: forecast_execution_datetime_utc
        description: "When PJM issued this forecast (UTC)."
      - name: forecast_execution_datetime_local
        description: "When PJM issued this forecast (local time, see timezone column)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: forecast_date
        description: "Target operating date being forecasted."
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'
      - name: region
        description: '{{ doc("col_region") }}'

  - name: pjm_gridstatus_load_forecast_hourly
    description: '{{ doc("pjm_mart_gridstatus_load_forecast_hourly") }}'
    columns:
      - name: forecast_execution_datetime_utc
        description: "When the forecast was issued (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: forecast_execution_datetime_local
        description: "When the forecast was issued (local time, see timezone column)."
      - name: forecast_rank
        description: '{{ doc("col_forecast_rank") }}'
      - name: forecast_date
        description: "Target operating date being forecasted."
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'
      - name: region
        description: '{{ doc("col_region") }}'

  - name: pjm_load_forecast_hourly_changes
    description: "PJM load forecast DA cutoff vintages (0h, -12h, -24h, -48h) by region. Latest forecast per vintage, forecast_date, hour_ending, and region for dates >= today."
    columns:
      - name: forecast_date
        description: "Target operating date being forecasted."
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'
      - name: region
        description: '{{ doc("col_region") }}'
      - name: vintage_label
        description: "DA cutoff vintage label (DA Cutoff, DA -12h, DA -24h, DA -48h)."
      - name: forecast_load_mw
        description: "Forecasted load (MW)."

  - name: pjm_outages_actual_daily
    description: '{{ doc("pjm_mart_outages_actual_daily") }}'
    columns:
      - name: date
        description: '{{ doc("col_date") }}'
      - name: region
        description: "PJM outage region."

  - name: pjm_fuel_mix_hourly
    description: '{{ doc("pjm_mart_fuel_mix_hourly") }}'
    columns:
      - name: datetime_beginning_utc
        description: "Start of the hour interval (UTC)."
      - name: datetime_ending_utc
        description: "End of the hour interval (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: datetime_beginning_local
        description: "Start of the hour interval (local time, see timezone column)."
      - name: datetime_ending_local
        description: "End of the hour interval (local time, see timezone column)."
      - name: date
        description: '{{ doc("col_date") }}'
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'

  - name: pjm_lmps_rt_hourly
    description: '{{ doc("pjm_mart_lmps_rt_hourly") }}'
    columns:
      - name: datetime_beginning_utc
        description: "Start of the hour interval (UTC)."
      - name: datetime_ending_utc
        description: "End of the hour interval (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: datetime_beginning_local
        description: "Start of the hour interval (local time, see timezone column)."
      - name: datetime_ending_local
        description: "End of the hour interval (local time, see timezone column)."
      - name: date
        description: '{{ doc("col_date") }}'
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'
      - name: hub
        description: "PJM pricing hub name."

  - name: pjm_load_rt_instantaneous_hourly
    description: '{{ doc("pjm_mart_load_rt_instantaneous_hourly") }}'
    columns:
      - name: datetime_beginning_utc
        description: "Start of the hour interval (UTC)."
      - name: datetime_ending_utc
        description: "End of the hour interval (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: datetime_beginning_local
        description: "Start of the hour interval (local time, see timezone column)."
      - name: datetime_ending_local
        description: "End of the hour interval (local time, see timezone column)."
      - name: date
        description: '{{ doc("col_date") }}'
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'
      - name: region
        description: '{{ doc("col_region") }}'
      - name: rt_load_mw
        description: "Instantaneous real-time load (MW), hourly average of 5-minute readings."

  - name: pjm_outages_forecast_daily
    description: '{{ doc("pjm_mart_outages_forecast_daily") }}'
    columns:
      - name: forecast_rank
        description: '{{ doc("col_forecast_rank") }}'
      - name: forecast_execution_date
        description: "Date the outage forecast run was issued."
      - name: forecast_date
        description: "Target operating date in the outage forecast horizon."
      - name: region
        description: "PJM outage forecast region."

  - name: pjm_tie_flows_hourly
    description: '{{ doc("pjm_mart_tie_flows_hourly") }}'
    columns:
      - name: datetime_beginning_utc
        description: "Start of the hour interval (UTC)."
      - name: datetime_ending_utc
        description: "End of the hour interval (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: datetime_beginning_local
        description: "Start of the hour interval (local time, see timezone column)."
      - name: datetime_ending_local
        description: "End of the hour interval (local time, see timezone column)."
      - name: date
        description: '{{ doc("col_date") }}'
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'
      - name: tie_flow_name
        description: "PJM tie flow interface name."

  - name: pjm_gridstatus_solar_forecast_hourly
    description: '{{ doc("pjm_mart_gridstatus_solar_forecast_hourly") }}'
    columns:
      - name: forecast_execution_datetime_utc
        description: "When the forecast was issued (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: forecast_execution_datetime_local
        description: "When the forecast was issued (local time, see timezone column)."
      - name: forecast_rank
        description: '{{ doc("col_forecast_rank") }}'
      - name: forecast_date
        description: "Target operating date being forecasted."
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'

  - name: pjm_gridstatus_wind_forecast_hourly
    description: '{{ doc("pjm_mart_gridstatus_wind_forecast_hourly") }}'
    columns:
      - name: forecast_execution_datetime_utc
        description: "When the forecast was issued (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: forecast_execution_datetime_local
        description: "When the forecast was issued (local time, see timezone column)."
      - name: forecast_rank
        description: '{{ doc("col_forecast_rank") }}'
      - name: forecast_date
        description: "Target operating date being forecasted."
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'

  - name: pjm_dates_daily
    description: '{{ doc("pjm_mart_dates_daily") }}'
    columns:
      - name: date
        description: '{{ doc("col_date") }}'

  - name: pjm_dates_hourly
    description: '{{ doc("pjm_mart_dates_hourly") }}'
    columns:
      - name: datetime_beginning_utc
        description: "Start of the hour interval (UTC)."
      - name: datetime_ending_utc
        description: "End of the hour interval (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: datetime_beginning_local
        description: "Start of the hour interval (local time, see timezone column)."
      - name: datetime_ending_local
        description: "End of the hour interval (local time, see timezone column)."
      - name: date
        description: '{{ doc("col_date") }}'
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'

  - name: pjm_ancillary_services_hourly
    description: '{{ doc("pjm_mart_ancillary_services_hourly") }}'
    columns:
      - name: datetime_beginning_utc
        description: "Start of the hour interval (UTC)."
      - name: datetime_ending_utc
        description: "End of the hour interval (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: datetime_beginning_local
        description: "Start of the hour interval (local time, see timezone column)."
      - name: datetime_ending_local
        description: "End of the hour interval (local time, see timezone column)."
      - name: date
        description: '{{ doc("col_date") }}'
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'
      - name: sr_price
        description: "MAD Synchronized Reserve clearing price ($/MWh). > $0 = scarcity adder active."
      - name: scarcity_adder_active
        description: "TRUE when reserve scarcity adder is firing."

  - name: pjm_reserves_hourly
    description: '{{ doc("pjm_mart_reserves_hourly") }}'
    columns:
      - name: datetime_beginning_utc
        description: "Start of the hour interval (UTC)."
      - name: datetime_ending_utc
        description: "End of the hour interval (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: datetime_beginning_local
        description: "Start of the hour interval (local time, see timezone column)."
      - name: datetime_ending_local
        description: "End of the hour interval (local time, see timezone column)."
      - name: date
        description: '{{ doc("col_date") }}'
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'
      - name: area
        description: "PJM reserve zone."
      - name: reserve_type
        description: "Reserve product type."
      - name: reserve_margin_mw
        description: "Reserve margin: quantity minus requirement (MW). Negative = deficit."
      - name: shortage_indicator
        description: "TRUE if shortage pricing was active."

  - name: pjm_lmps_rt_five_min
    description: '{{ doc("pjm_mart_lmps_rt_five_min") }}'
    columns:
      - name: datetime_beginning_utc
        description: "Start of the 5-minute interval (UTC)."
      - name: timezone
        description: "IANA timezone of all local timestamps (e.g., US/Eastern)."
      - name: datetime_beginning_local
        description: "Start of the 5-minute interval (local time, see timezone column)."
      - name: date
        description: '{{ doc("col_date") }}'
      - name: hour_ending
        description: '{{ doc("col_hour_ending") }}'
      - name: hub
        description: "PJM pricing hub name."
      - name: rt_lmp
        description: "5-minute real-time LMP ($/MWh)."
